{
    "name": "nonojs",
    "private": "true",
    "workspaces": [
        "common",
        "client",
        "server"
    ],
    "scripts": {
        "clean": "npm run clean:remove && npm run build",
        "clean:remove": "rm -r client/dist/ && rm -r server/dist/ && rm -r common/dist/",
        "build": "npm run build --ws",
        "start": "npm run start -w server",
        "dev": "concurrently \"npm run dev --workspace common\" \"npm run dev --workspace client\" \"npm run dev --workspace server\""
    },
    "dependencies": {
        "concurrently": "^9.2.1"
    }
}
